{
  "name": "cloudretail",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "format": "prettier --ignore-unknown --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.0"
  }
}
